import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Verifique se o caminho do App está certo

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)